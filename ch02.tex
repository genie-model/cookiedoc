%----------------------------------------------------------------------------------------
%       CHAPTER 2
%----------------------------------------------------------------------------------------

\cleardoublepage

\chapterimage{amoc.png} % Chapter heading image

\chapter{Ocean circulation I -- modern}\label{ch:ocean-circulation-I}

\hfill \break

\noindent Stuff to keep in mind:

\begin{itemize}
\item Nothing at all – keep your mind completely empty and let the wonderful truths of \textbf{muffin} permeate your entire being.
\end{itemize}

\vspace{2mm}
\noindent Background reading (Atlantic circulation and stability in \textbf{muffin}):

\vspace{2mm}
\begin{itemize}
\item Hargreaves et al. [2004] (Climate Dynamics 23, 2004, Pages 745 – 760)
\\\(\rightarrow\)Simple assessment of the likelihood of AMOC collapse.
\item Marsh et al. [2004] (Climate Dynamics, 23 2004, Pages 761 – 777)
\\\(\rightarrow\)Characterization of thresholds of AMOC collapse.
\item Singaraye et al. [2008] (GRL 35, doi:10.1029/2008GL034074)
\\\(\rightarrow\)Role of changing ocean circulation in atmospheric radiocarbon variability during the Younger Dryas.
\end{itemize}

\vspace{2mm}
\noindent Background reading (Miscellaneous (model) Atlantic circulation and stability):

\vspace{2mm}
\begin{itemize}
\item IPCC [2007] (e.g., Section 10.3.4)
\\\(\rightarrow\)Future predictions of AMOC strength.
\item Schmittner [2005] (Nature 434, 628– 633)
\\\(\rightarrow\)Impacts on marine ecosystems and carbon cycling.
\item Obata [2007] (J. Clim. 20, 5962–5976)
\\\(\rightarrow\)Climate-carbon cycle model response to freshwater discharge.
\end{itemize}

%------------------------------------------------
\newpage
%------------------------------------------------

\section*{READ.ME}

You will need to download new \textit{re-start} files prior to embarking on the experiments with modern ocean circulation. To fetch them: change to the \textsf{\footnotesize cgenie\_output} directory, and type (or copy and paste carefully from the PDF ...):

\vspace{-2mm}\small\begin{verbatim}
$ wget --no-check-certificate http://www.seao2.info/cgenie_output/
   cookie.C.p_worjh2.r.SPIN.tar.gz
\end{verbatim}\normalsize\vspace{-2mm}
(single line ... no space between line fragments ...) This downloads  archived/compressed copies of the 10,000 year \textit{spin-up} experiments \textsf{\footnotesize cookie.C.p\_worjh2.r.SPIN}. Extract the contents by typing:

\vspace{-2mm}\small\begin{verbatim}
$ tar xfzv cookie.C.p_worjh2.r.SPIN.tar.gz
\end{verbatim}\normalsize\vspace{-2mm}
You will then need to change directory back to \textsf{\footnotesize genie-main} to run the model.

%------------------------------------------------
\newpage
%------------------------------------------------

\section{Simulating ocean circulation}

\textbf{Important}.

\begin{enumerate}[noitemsep]
\vspace{1mm}
\item The \textbf{cookie} configuration used in this exercise is a seasonally-forced modern continental configuration with 16 vertical levels in the ocean. This is indicated by the code '\textsf{\footnotesize p\_worjh2}' in the \textit{base-config} file-name. The grid and definition of the vertical levels are illustrated in Figure \ref{fig:ch3-ijgrid}) and (Figure \ref{fig:ch3-kgrid}), respectively.
\vspace{1mm}
\item The configuration does not have a carbon cycle, but does include a numerical 'age' tracer to help diagnose ocean circulation and rate of ventilation of the deep ocean. This is indicated by the code '\textsf{\footnotesize r}' in the \textit{base-config} file-name.
\vspace{1mm}
\item Make use of \textit{control} experiments in order to create anomaly maps (see later). An ideal control experiment is one that is run for as long as your 'real' (perturbation) experiments, but has no changes made to the \textit{user-config} (compared to the \textit{re-start}).
\end{enumerate}

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\begin{figure}[H]
\includegraphics[width=0.8\textwidth]{ch3-ijgrid.png}\centering
\vspace{2mm}
\caption{
\textbf{The \textbf{cookie} grid for a modern \(36\times36\) ‘p\_worjh2’ configuration.} Light blue numbers are the ‘i’ co-ordinates. Green numbers are the ‘j’ co-ordinates.
The depth of the ocean at any location is indicated by its ‘k’ value – a number between 1 and 16, with 16 being the surface layer of the ocean, and 1 the maximum possible depth anywhere.
Numbers > 90 (i.e., 91, 92, 93, 94) and shaded grey are land (the numbers specify the compass direction of rainfall run-off). The longitude of the western edge of this particular modern ocean grid is at 260$^{\circ}$W, and the increments are 10$^{\circ}$.
}
\label{fig:ch3-ijgrid}
\end{figure}

\begin{figure}[H]
\includegraphics[width=0.4\textwidth]{ch3-kgrid.png}\centering
\vspace{-0mm}
\caption{\textbf{The \textbf{cookie} ocean vertical level definitions for a modern 16-level ocean grid.}}
\label{fig:ch3-kgrid}
\end{figure}

%------------------------------------------------
\newpage
%------------------------------------------------

\section{Visualizing ocean circulation}

Visualizing the 3D flow (/transport) of the ocean, much less the rate at which this occurs, is no trivial matter. Even with the aid of a model. (Or rather, the problem then becomes: how to visualize ocean circulation in a model.) We'll consider three different ways of analyzing model fields -- read this description \uline{first}, and then run an experiment to try the visualization techniques on.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent You can deduce something about ocean circulation from the 3D patterns of salinty (variable: \textsf{\footnotesize ocn\_sal}), temperature (variable: \textsf{\footnotesize ocn\_temp}), and/or density (variable: \textsf{\footnotesize climate\_rho}). For example, from \textsf{\footnotesize fields\_ocean\_3d.nc} -- plotting a latitude-depth section through the Atlantic along latitude -25$^{\circ}$E (remember that \textbf{Panoply} plots a global zonal mean by default). If you plot both salinity and temperature you can try and identify the dominant regimes of NADW (warm, salty) vs. AABW (cold, fresh).

You can also contrast the model with observations. Download the following 2 \textbf{netCDF} files -- they are of climatological ocean temperature and salinity (World Ocean Atlas), re-gridded to the \textbf{cookie} ocean grid and can be visualized in the exact same way as \textbf{cookie} output.

\vspace{-2mm}\small\begin{verbatim}
$ http://www.seao2.info/data/worjh2.temperature.260123.nc
$ http://www.seao2.info/data/worjh2.salinity.260123.nc
\end{verbatim}\normalsize\vspace{-2mm}

Because model and observations share the same grid, you can make direct comparisons between plotted fields, including creating difference maps of model minus observations.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent The second approach is to simply visualize the raw velocity fields, but plotted as ocean currents.\vspace{1mm}
\begin{enumerate}[noitemsep]
\vspace{1mm}
\item  In the 3D \textbf{netCDF} file, the three components of ocean velocity are represented by the variables: ocean velocity – u (Eastwards), ocean velocity – v (Northwards), and ocean velocity – w (upwards). 2. Open up velocity – u. Chose ‘lon-lat’.
\vspace{1mm}
\item Select/highlight velocity – v. and click on the ‘Combine Plot’ icon (as per before).
\vspace{1mm}
\item Rather than a difference map, which is what you get by default, i.e., ‘Array 1 – Array 2’ – from the drop-down menu (next to the ‘Interpolate’ button) select ‘Vector Magnitude’.
\vspace{1mm}
\item You should have a color contoured (or not if you prefer plotting without contouring on) map of ocean current speed, with velocity vectors (direction and magnitude) overlain. You’ll need to re-scale the velocity vectors to properly see them – from the \textsf{\footnotesize Contours and Vectors} tab – change the \textsf{\footnotesize Scale Length} to e.g., $0.15$ (for typical surface velocities). You can also increase the density of vectors by decreasing \textsf{\footnotesize Spacing} to $50\%$.
\\When fresh-water hosing – look out for impacts on the N. Atlantic current system associated with the AMOC.
\vspace{1mm}
\item You can repeat this for deeper depth levels in the ocean – e.g., $2000-3000 m$ is a good place to go looking for the Deep Western Boundary Current (and AMOC return) in the model (such as it exists at this low resolution), but you’ll need to re-scale the velocity vectors again (e.g., to $0.1$ to less).
\end{enumerate}
\vspace{1mm}

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\noindent An example plot (using \textbf{Panoply} for visualizing surface ocean current fields) is given in Figure \ref{fig:ch2-currents_0050m} and for $2750 m$ in Figure \ref{fig:ch2-currents_2750m}.

\begin{figure}[H]
\includegraphics[width=0.6\textwidth]{ch2-currents_0050m.png}\centering
\vspace{-0mm}
\caption{Example plot of (normal/default modern) ocean surface ($40 m$) current fields.}
\label{fig:ch2-currents_0050m}
\end{figure}

\begin{figure}[H]
\includegraphics[width=0.6\textwidth]{ch2-currents_2750m.png}\centering
\vspace{-0mm}
\caption{Example plot of (normal/default modern) deep ($2750 m$) ocean current fields.}
\label{fig:ch2-currents_2750m}
\end{figure}

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\noindent The third approach is to visualize the large-scale ocean transport in terms of the meridional overturning circulation ('stream-function') (e.g. see background literature). Two example plots (using \textbf{Panoply}) are shown in Figure \ref{fig:ch3-amoc1} for the Atlantic basin, and Figure \ref{fig:ch3-amoc2} for the Pacific. In the In the 2D \textbf{netCDF} file, relevant fields (\textbf{netCDF} variables)\footnote{Note that these fields are only meaningful for the modern arrangement of the continents and a continuous separation of the Atlantic from the Pacific from high northern latitudes down to the tip of South America. Different (e.g. paleo) arrangements of the continents may not have recognizable (or definable) Atlantic and/or Pacific basins and it may only be possible to define and visualize the global meridional overturning circulation -- variable: \textsf{\footnotesize phys\_opsi}.} are:

\vspace{1mm}
\begin{itemize}[noitemsep]
\setlength{\itemindent}{.0in}
\item [] \textsf{\footnotesize climate\_opsi}  == global overturning stream-function
\item [] \textsf{\footnotesize climate\_opsia} == overturning in the Atlantic
\item [] \textsf{\footnotesize climate\_opsip} == overturning in the Pacific
\end{itemize}

\begin{figure}[H]
\includegraphics[width=0.6\textwidth]{ch3-amoc1.png}\centering
\vspace{-2mm}
\caption{Example plot of (normal/default modern) overturning stream-function (2D \textbf{netCDF} file). (e.g., for Atlantic: \textbf{netCDF} parameter name: \texttt{phys\_opsia}, long-name: Atlantic stream-function). Note that auto-scaling has been turned off and the min and max plotting limits set manually. By convention, stream-functions are plotted with their scale symmetrical around zero, giving red and ‘warm’ colors for positive value and clockwise overturning, and blues and ‘cold’ colors for negative values and anti-clockwise overturning.}
\label{fig:ch3-amoc1}
\end{figure}

\begin{figure}[H]
\includegraphics[width=0.6\textwidth]{ch3-amoc2.png}\centering
\vspace{-2mm}
\caption{Pacific meridional overturning circulation (PMOC).}
\label{fig:ch3-amoc2}
\end{figure}

%------------------------------------------------
\newpage
%------------------------------------------------

\noindent To start off -- run a \textit{control} experiment using the \textit{re-start} experiment that you have just downloaded as the initial state of your experiment. A \textit{control} experiment is basically one in which you do not change any parameter settings and is used top quantify any residual drift (that you might otherwise interpret as the result of a parameter change in your actual experiment). The template \textit{user-config} file you are provided for, for this, is: \textsf{\footnotesize LABS.2.1}

\vspace{1mm}
Run the model for … whatever, 50 years will do for now:

\vspace{-2mm}\small\begin{verbatim}
$ ./runcookie.sh cookie.C.p_worjh2.r LABS LAB.2.1 50 cookie.C.p_worjh2.r.SPIN
\end{verbatim}\normalsize\vspace{-2mm}
where \textsf{\footnotesize cookie.C.p\_worjh2.r.SPIN} is the \textit{re-start} experiment name.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\section{Tracing ocean ventilation}

Another way to quantify global ocean circulation is through considering the connection (rate of mass exchange) between surface and deep ocean -- 'ventilation'.

\textbf{cookie} has the capability to employ/simulate a ventilation age tracer\footnote{Under the '\textit{screw with and/or diagnose the climate system}' HOW-DO -- see '\textit{Add a water mass age tracer}' (and the 'easy'/automatic method described towards the end of that section).} -- a numerical tracer carried in the ocean circulation model that tracks the time since a parcel of water last 'saw' the surface. The older the 'age' of the parcel of water, the longer the time since it last saw the surface. (Basically, we let the model automatically restore the tracer value at the surface to zero and everywhere else (in the ocean interior) increase the age each time-step (by the duration of the time-step) such that a parcel of water away from the surface ages by 1 year, each year.)

\vspace{1mm}

In the 3D netCDF output file -- \textsf{\footnotesize climate\_age} is the output variable that is the calculated ventilation age.

In the time-series -- the mean deep ocean (>2000 m) water-mass age is file:
\vspace{1mm}
\\\textsf{\footnotesize timeseries\_climate\_ventilation\_age.txt}.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent Explore the distribution of water mass age and think about the physical ocean circulations reasons for this. Are all the modeled distributions reasonable? Are there indicators of facets of the simulated circulation that are not particularly realistic? Try plotting both lon-lat and lat-depth slices through the ocean. How does the distribution of water mass age relate to the overturning stream-function for Atlantic or Pacific basins?

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\section{Poking the climate beast (1)}

\textbf{cookie} provides a framework for applying time- and spatially-variable ‘forcings’ of the Earth system\footnote{Refer to the '\textit{force the system}' \textsf{HOW-TO} in the cookie manual for further details on \textit{forcings}.} – fluxes or 'restored-to' boundary conditions that can be prescribed for any gas, dissolved substance (including temperature and salinity), or particulate matter. Examples include freshwater input (== a negative salinity flux forcing) of the North Atlantic to alter ocean circulation, fossil fuel \(CO_{2}\) emissions to the atmosphere (== a \(CO_{2}\) gas flux forcing), or aeolian iron supply to the surface ocean (a 2-D dust flux forcing). In this exercise you are going to add fresh water to the ocean surface to assess the sensitivity of the Atlantic Meridional Overturning Circulation (AMOC) to collapse, in a classic ‘hosing’ experiment. 

\vspace{1mm}
The \textit{user-config} file for this is called: \textsf{\footnotesize LAB.2.4}. This includes a prescribed transport of salt to the North Atlantic (north of ca. 30$^{\circ}$N) from the result of the global surface ocean (for mass balance). This is specified by the line:

\vspace{-2mm}\small\begin{verbatim}
bg_par_forcing_name="p_worjh2.Fsal_SUR_NAtl"
\end{verbatim}\normalsize\vspace{-2mm}

This is paired with a scaling parameter for ease of changing the magnitude of the flux forcing:

\vspace{-2mm}\small\begin{verbatim}
bg_par_ocn_force_scale_val_2=0.0
\end{verbatim}\normalsize\vspace{-2mm}

\noindent which by default is zero (i.e., no flux forcing).

\vspace{1mm}
To orientate you in freshwater forcing space: \texttt{\small bg\_par\_ocn\_force\_scale\_val\_2=-1.0} ($1Sv$ = $10^{6} m^{3} s^{-1}$) should be sufficient to make ‘stuff happen’ and quickly. BUT, this is a pretty extreme flux (contrast with the average Amazon freshwater transport to the Atlantic of $0.22-0.25Sv$). Much more than this and the model may crash or at the very least, you might be left with a large freshwater pond in the North Atlantic. Don't forget the minus sign if you want a net freshwater flux to the N Atlantic rather than a salinity one ...

\vspace{1mm}
To run the model for 50 years using the same \textit{re-start} as previously:

\vspace{-2mm}\small\begin{verbatim}
$ ./runcookie.sh cookie.C.p_worjh2.r LABS LAB.2.4 50 cookie.C.p_worjh2.r.SPIN
\end{verbatim}\normalsize\vspace{-2mm}

\noindent 50 years should be more than long enough to see a collapse to occur, but running for longer than this will allow you to have a smaller, less extreme (and maybe more realistic) freshwater input flux and still see an impact occur.

Make sure that you run a \uline{control} experiment -- an experiment of the same duration of your hosing experiments, but with a zero freshwater flux. The impact of freshwater input, is the difference, at the same model year, between the perturbation experiment and the control. (You only ever need need to run one control, regardless of how many different freshwater flux perturbation experiments  you run.) Remember that you can make use of cluster queuing possibilities and run multiple experiments at once in the background.

Note that as provided, \textsf{\footnotesize LAB.2.4} is set up to save \textbf{netCDF} \textit{time-slices} every 10 years -- the parameter setting:

\vspace{-2mm}\small\begin{verbatim}
bg_par_data_save_slice_timeinterval=10.0
\end{verbatim}\normalsize\vspace{-2mm}

Change this to have fewer or more \textit{time-slices} saved (setting to zero will result in a \textit{time-slice} saved only in the very last year of the experiment, regardless of how long it is run)

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\noindent \textbf{What to look for?} The most obvious property of the Earth system to follow is the Atlantic overturning strength (\textsf{\footnotesize timeseries\_climate\_opsi.txt}). The AMOC stream-function (in \textsf{\footnotesize fields\_ocean\_2d.nc} 2-D time-slice \textbf{netCDF} results file, field: \textsf{\footnotesize climate\_opsia}) is also illustrative. You can also try and identify the salinity anomaly (see below) due to freshwater input in the 3D salinity tracer field.

There are also important (but not necessarily painfully exciting) impacts on surface air temperatures and maybe sea-ice extent (in \textsf{\footnotesize fields\_ocean\_2d.nc}) (but see below for a better way to visualize these changes). Note the importance (sort of) of the AMOC in transporting heat to the N Atlantic region (the film the Day After Tomorrow was not entirely inaccurate in this particular respect). Be aware of the possibility of climate impacts far from the location of fresh water forcing. Look out for any significant-looking impacts on sea-ice extent, etc.

You might plot current velocity fields and visualize how these change in response to the fresh water forcing, and if you ran much longer than 10 years, see impacts on ocean ventilation ages.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent To more easily assess some of these impacts (and for other sorts of analysis) it is possible to create an \uline{anomaly (difference) map} in \textbf{Panoply}:

\vspace{1mm}
\begin{enumerate}[noitemsep]
\vspace{1mm}
\item  First open a dataset, e.g., \textsf{\footnotesize climate\_SAT} (surface air temperature) in the 2D \textbf{netCDF} file. Double-click the variable name, or, with the variable name highlighted, click the ‘Create Plot’ icon.
\vspace{-3mm}
\item Now, with the \textsf{\footnotesize climate\_SAT} still selected (and the first plot window still open), click on the ‘Combine Plot’ icon. A dialogue box will appear and ask you to select a plot to combine the new one with. Make sure the name of your first plot window is selected/highlighted. Click ‘Combine’. OR, simply drag a second dataset into the plot window of the first dataset.
\vspace{1mm}
\item You now have a plot window that by default it is showing you the difference between two identical (in time) slices. The two slices are labeled Array 1 (LH side) and Array 2 (RH side).
\end{enumerate}
\vspace{1mm}

Keep one array (Array 1) fixed to the initial (year 10 (centered on 9.5)) and vary the year in the second array (Array 2). Note that you can select in Panoply whether Array 1 – Array 2 is plotted, or Array 2 – Array 1, or various proportional or relative differences.

Note that you can switch off the auto-scaling feature (Always fit to data) and center the scale so that no change is white, with positive deviations = red and negative = blue by clicking on Center on 0 (an often used convention in climate field plotting).

\vspace{1mm}
This is a pretty crude way of visualizing changes in an experiment because in your case, year 10 already encapsulates a significant climate perturbation from the freshwater hosing. \textbf{Best practice is to open your experiment netCDF \uline{and} your control experiment, and difference the same year of experiment minus control.}

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent Try and answer the question: \textbf{How much fresh water flux (in Sv) is required to collapse the AMOC?} and how non-linear is the response of overturning to fresh water flux?

You will need to submit multiple experiment jobs to the cluster queue, with each experiment differing in the assumed radiative forcing parameter value. Plot the strength of the AMOC that is reached at the end of each experiment (from the time-series file: \textsf{\footnotesize timeseries\_climate\_opsi.txt}) vs. radiative forcing.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\section{Poking the climate beast (2)}

A current concern regarding anthropogenic climate change is the ocean circulation (and marine ecological and biogeochemical) response to a strong warming of the surface, as rapid surface warming is assumed (and demonstrated in models) to result in surface stratification of the ocean, likely restricting the nutrient supply to phytoplankton and reducing ventilation of the ocean interior with dissolved oxygen.

You can explore the transient response of ocean circulation to warming by simply adjusting the radiative forcing parameter as per used in the snowball Earth experiments: \texttt{ea\_radfor\_scl\_co2}. By default in the modern continental configuration, this has a value of 1.0, corresponding to 278 ppm atmospheric \(CO_{2}\). A value of 2.0 would reflect warming equivalent to 556 ppm \(CO_{2}\). And 3.0 would be more like an end-of-the-century warming. Note that you are applying the warming instantaneously by manipulating the climate system in this way and hence the changes will be more extreme than those occurring over the time-scale of this century. Also note that a cooling (a radiative scaling smaller than $1.0$) could be applied instead.

The same parameter as before appears at the start of the \textit{user-config} provided for this Section (\textsf{\footnotesize LAB.2.5}) for you to make this radiative forcing adjustment:

\vspace{-2mm}\footnotesize\begin{verbatim}
# SPECIFY RADIATIVE FORCING TO BE APPLIED
# NOTE: the value of ea_radfor_scl_co2 represents a multiplier of CO2 relative to 278 ppm 
#       and radiative forcing is derived from this
# e.g., ea_radfor_scl_co2=1.0
#       -> radiative forcing equivalent to changing from x1 to x1 preindustrial CO2 (no change!)
ea_radfor_scl_co2=1.0
\end{verbatim}\normalsize\vspace{-2mm}

Remember that it is best practice to copy-rename-edit \textsf{\footnotesize LAB.2.6.warming} to create a new and unique experiment name for a new experiment.

\vspace{1mm}
You are running your new experiment from the same \textit{re-start} state as before, e.g.:

\vspace{-2mm}\small\begin{verbatim}
$ ./runcookie.sh cookie.C.p_worjh2.r LABS LAB.2.5 50 cookie.C.p_worjh2.r.SPIN
\end{verbatim}\normalsize\vspace{-2mm}

Potentially interesting properties of the Earth system to look at include sea-ice extent and AMOC strength (in the ASCII \textit{time-series} file), and the overturning stream-function (2D with \textbf{netCDF} variable \textsf{\footnotesize climate\_opsia}) and sea-ice extent in the 2-D \textbf{netCDF} output. With the age tracer included you can also make projections of how patterns of ventilation age change with transient warming.

Finally -- is the spatial pattern of warming even? You will need to create a difference map of surface temperature -- variable \textsf{\footnotesize climate\_SAT} in \textsf{\footnotesize fields\_ocean\_2d.nc} -- to be able to see this well.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent Try and answer the question: \textbf{How much radiative forcing is required to collapse the AMOC?} (and how much atmospheric \(CO_{2}\) does this approximately correspond to?) How non-linear is the response of overturning to fresh water flux?

You will need to submit multiple experiment jobs to the cluster queue, with each experiment differing in the assumed radiative forcing parameter value. Plot the strength of the AMOC that is reached at the end of each experiment (from the time-series file: \textsf{\footnotesize timeseries\_climate\_opsi.txt}) vs. radiative forcing.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\section{Mediterranean-Atlantic circulation}

%------------------------------------------------

In this section we will explore some of the controls, and ways to further visualize, seawater exchange between the Atlantic Ocean and Mediterranean Sea.

\subsection{Poking the Mediterranean}

In the first of a series of Mediterranean-focused exercises, you will visualize the simulated circulation between the Mediterranean Sea and Atlantic, and explore the impact of changing the water balance in the Mediterranean Sea (by means of an applied salinity forcing), not only on Mediterranean salinity, but on the AMOC (and even global climate). 

The experiment configuration (\textit{user-config}) -- \textsf{\footnotesize LAB.2.6.1} -- in the \textsf{\footnotesize genie-userconfigs/LABS} directory is almost identical to all the other ones in this Chapter, except that it prescribes a net salt transport into the Eastern Mediterranean (the South-Eastern-most 5 Mediterranean cells in the model grid -- see Figure 2.1) with a balancing freshwater flux distributed across the remainder of the global ocean surface (so that globally, salinity is neither lost not gained, just re-distributed). This is implemented by the \textit{forcing}:

\vspace{-2mm}\small\begin{verbatim}
bg_par_forcing_name="p_worxx2.Fsal_SUR_EMed"
\end{verbatim}\normalsize\vspace{-2mm}

\noindent This setting is paired with a scaling parameter for changing the magnitude of the flux forcing:

\vspace{-2mm}\small\begin{verbatim}
bg_par_ocn_force_scale_val_2=0.0
\end{verbatim}\normalsize\vspace{-2mm}

\noindent which by default is zero (i.e., no flux forcing). Setting the value of this parameter, for example, to: \\\texttt{\small bg\_par\_ocn\_force\_scale\_val\_2=1.0} would create a net freshwater removal from the Eastern Mediterranean of $1Sv$ (probably the largest you want to try). Note that you need to apply a value that is \uline{positive} so that we get a salt addition rather than freshwater addition in the Eastern Mediterranean.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent To start with -- run a \uline{control} experiment with no salinity forcing (\texttt{\small bg\_par\_ocn\_force\_scale\_val\_2=0.0}) so that you create a record of the background (un-adjusted) state of ocean circulation. Copy-rename \textit{user-config} file \textsf{\footnotesize LAB.2.6.1}, ideally choosing a filename that helps you remember what the experiment was (e.g., \textsf{\footnotesize LAB.2.6.1.control}, and try running this for e.g., 50 years to start with, following on from the \textit{restart} experiment you downloaded (\textsf{\footnotesize cookie.C.p\_worjh2.r.SPIN}), i.e.,

\vspace{-2mm}\small\begin{verbatim}
$ ./runcookie.sh cookie.C.p_worjh2.r LABS LAB.2.6.1.control 50
    cookie.C.p_worjh2.r.SPIN
\end{verbatim}\normalsize\vspace{-2mm}
(all one line)

\vspace{1mm}
\noindent \textbf{What to look for?} Start by viewing time-series of the maximum positive strength of Atlantic Meridional Overturning Circulation (AMOC), global mean annual surface sea and/or air temperature (and perhaps \% sea-ice cover). These are the files\footnote{In your experiment \textsf{\footnotesize results} directory.}, respectively:

\vspace{1mm}
\begin{itemize}[noitemsep]
\setlength{\itemindent}{.0in}
\item [] \textsf{\footnotesize timeseries\_climate\_opsi.txt}
\item [] \textsf{\footnotesize timeseries\_climate\_SST.txt}
\item [] \textsf{\footnotesize timeseries\_climate\_SAT.txt}
\item [] \textsf{\footnotesize timeseries\_climate\_seaice.txt}
\end{itemize}

\vspace{1mm}
Confirm that as the experiment runs, none of the displayed global climate state variables 'drift' significantly. (If they do, it might indicate that either you are using the wrong \textit{restart} experiment, or you have a parameter change in the experiment that you were not intending to use.)

%------------------------------------------------
\newpage
%------------------------------------------------

\noindent Next create the following plots:

\begin{enumerate}[noitemsep]
\setlength{\itemindent}{.0in}
\vspace{1mm}
\item The ocean surface current velocity field -- refer to Figure along with the associated text.
\vspace{1mm}
\item The stream-function of the AMOC -- refer to Figure \ref{fig:ch3-amoc1} along with the associated text.
\vspace{1mm}
\item The distribution of ocean surface SST - a lon-lat plot of the ocean surface layer in the model from: \textsf{\footnotesize fields\_ocean\_3d.nc} (or from: \textsf{\footnotesize fields\_ocean\_2d.nc}).
\vspace{1mm}
\item The vertical distribution of salinity through the Mediterranean and Atlantic.\footnote{Note that you can identify Mediterranean outflow in a carefully-chosen section -- pick a latitude-depth section close to the N. Africa margin in the model grid (e.g.,-15$^{\circ}$E or -5$^{\circ}$E), in a sub-surface horizontal (longitude-latitude) section (e.g., between around 500 and 1000 m depth), or best, in a longitude-depth section cutting across the Atlantic an through the Mediterranean Sea (e.g., latitude of around 35$^{\circ}$N).} 
\\For this, you want field: \textsf{\footnotesize climate\_sal} in \textsf{\footnotesize fields\_ocean\_3d.nc}. Choose a \textsf{\footnotesize longitude-vertical} plot in the opening \textbf{Panoply} dialogue box. Then navigate to the \textsf{\footnotesize Latitude of the t grid} item at the bottom of the \textsf{\footnotesize Plot Controls} dialogue box, and choose \textsf{\footnotesize 35.86} degrees North. Change \textsf{\footnotesize Plot Controls} from \textsf{\footnotesize Array} to \textsf{\footnotesize Scale} and hit \textsf{\footnotesize Fit to Data}. You should see something like Figure \ref{fig:ch2-Med_salinity_section}. (You might want to adjust how the salinity scale labels are printed, pick simple min and max scale values, and set a sensible number of graduations.)
\end{enumerate}

\vspace{1mm}
Save these images to file (and label as 'control') as you will need to evaluate the results of perturbation experiments against them shortly. (You can also create difference maps of experiment vs. control for everything except the current velocity field.)

\begin{figure}
\includegraphics[width=0.6\textwidth]{ch2-Med_salinity_section.png}\centering
\vspace{-0mm}
\caption{Example plot of (normal/default modern) salinity section through the Mediterranean and Atlantic}
\label{fig:ch2-Med_salinity_section}
\end{figure}

\vspace{1mm}
\noindent \textbf{Q.} What can you tell from this section about seawater exchange between Mediterranean and Atlantic? Does the Mediterranean influence the seawater (and hence presumably density) properties of the Atlantic in any way? If so, what implications might there be for large-scale/global ocean circulation patterns and climate?

\vspace{1mm}
You might also plot a velocity field for a depth level just a little shallower than the sill depth, such as $500 m$ (see: Figure \ref{fig:ch2-currents_0500m}), to better try and visualize Mediterranean outflow.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\noindent Finally, for evaluating the model simulation of modern ocean circulation and salinity distributions, be sure to contrast the model with observations. You can obtain a \textbf{netCDF} files or climatological ocean salinity (World Ocean Atlas), re-gridded to the \textbf{cookie} ocean grid, here:
\vspace{-2mm}\small\begin{verbatim}
$ http://www.seao2.info/data/worjh2.salinity.260123.nc
\end{verbatim}\normalsize\vspace{-2mm}

\vspace{1mm}
\noindent \textbf{Q.} What does the model do 'OK', and what does it not do so well at (in terms of reproducing observations). Do you think model-data misfits will 'matter' (and hence potentially bias your model results)? Which model biases might be more important/relevant?

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent Now you are ready to perturb the system in a new experiment, and hence, copy-paste-rename the \textit{user-config} once more (here, for example: \textsf{\footnotesize LAB.2.6.1.1p0Sv} so as to indicate that the flux in the experiment will be $1.0$). Again, try and pick a filename that will help you later on to remember which experiment was what and not to mix up experiments and come to weird incorrect conclusions :o) For example, to prescribe a $1.0 Sv$ salinity anomaly in the Eastern Mediterranean:

\vspace{-2mm}\small\begin{verbatim}
# scale the salinity forcing in units of Sv
# NOTE: to represent freshwater (rather than salinity) addition
bg_par_ocn_force_scale_val_2=1.0
\end{verbatim}\normalsize\vspace{-2mm}
and make sure that the duration (time scaling) is at least as long as your experiment:
\vspace{-2mm}\small\begin{verbatim}
# scale the duration of the forcing (years)
bg_par_ocn_force_scale_time_2=50.0
\end{verbatim}\normalsize\vspace{-2mm}

Then running it:

\vspace{-2mm}\small\begin{verbatim}
$ ./runcookie.sh cookie.C.p_worjh2.r LABS LAB.2.6.1.1p0Sv 50
    cookie.C.p_worjh2.r.SPIN
\end{verbatim}\normalsize\vspace{-2mm}
(all one line)

You should see a rich (complicated!) response of the model with time as it adjusts to adding salt to the Eastern Mediterranean (although we could equally apply it in a different pattern). Initially, the AMOC weakens because you are taking salt away from everywhere including the N. Atlantic and redistributing it to the Mediterranean. But by year ca. 15, something 'interesting' starts to happen ... 

\vspace{1mm}
\noindent \textbf{Q.} Why does the model do what it does? You will need to repeat some or all of the plotting analyses you have carried out previously (and view the key \textit{time-series} output as well).
\\\noindent \textbf{Q.} Do you see evidence for an impact on global climate ... \textbf{why}??? How can this possibly happen just from redistributing salinity?

\vspace{1mm}
\noindent Lastly -- try running the model for longer, e.g., for $100$ years, but leaving the parameter setting \texttt{\small bg\_par\_ocn\_force\_scale\_time\_2=50.0}, so that you have salinity forcing applied for $50$ years, and then no forcing for a further $50$.

\vspace{1mm}
\noindent \textbf{Q.} What happens (in terms of e.g., AMOC) when the salinity forcing is removed? (A more difficult question is why?)

(Ideally, you would then run a new control experiment of the same length, as well as creating a new \textit{user-config} file and hence experiment name.)

%------------------------------------------------
\newpage
%------------------------------------------------

\begin{figure}[H]
\includegraphics[width=0.6\textwidth]{ch2-currents_0500m.png}\centering
\vspace{-0mm}
\caption{Example plot of (normal/default modern) intermediate depth ($500 m$) ocean current fields.}
\label{fig:ch2-currents_0500m}
\end{figure}

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\subsection{Estuarine  vs. anti-estuarine  circulation}

Exchange of seawater between Atlantic and Mediterranean consists of flow at the surface from Atlantic to Mediterranean, together with outflow from Mediterranean to Atlantic at depth (and over the sill) -- an 'anti-estuarine' mode of circulation. In your experiments involving additional salt transfer into the Mediterranean, this flow becomes stronger.

In theory, seawater could flow out of the Mediterranean at the surface, and in from the Atlantic at depth -- an 'estuarine' mode of circulation. However, deep Mediterranean seawater would presumably have to be fresher (and/or warmer) than the adjacent Atlantic seawater for an Easterly flow at depth (and Westerly at the surface).

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent You can make the Mediterranean fresher by making the salinity scaling negative. For example, to prescribe a $1.0 Sv$ \uline{freshwater} anomaly in the Eastern Mediterranean:

\vspace{-2mm}\small\begin{verbatim}
# scale the salinity forcing in units of Sv
# NOTE: to represent freshwater (rather than salinity) addition
bg_par_ocn_force_scale_val_2=-1.0
\end{verbatim}\normalsize\vspace{-2mm}
(And again make sure that the duration (time scaling) value of parameter \\\texttt{\small bg\_par\_ocn\_force\_scale\_time\_2} is at least as long as your experiment duration.)

\vspace{1mm}
\noindent \textbf{TO-DO:} By running multiple experiments (you can base these on \textsf{\footnotesize LAB.2.6.1}) all differing in the value of the freshwater flux forcing (and \textit{user-config} filename!), identify the point at which circulation switches from anti-estuarine to estuarine circulation. How much fresher would the Mediterranean have to be to support an estuarine mode of circulation? What is the flux forcing (in $Sv$) that corresponds to this? Is this a something that could have plausibly occurred in the past??

\vspace{1mm}
\noindent Remember: \uline{multiple experiments can be run simultaneously by submitting to the cluster queue}.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\subsection{Exploring the role of Gibraltar Strait sill depth}

In the standard modern configuration of \textbf{cookie} -- See Figure \ref{fig:ch3-ijgrid} -- the Strait of Gibraltar is a single model grid point wide (and even then, wider than it should be at the present-day), and has a depth of $558 m$. The distribution of land vs. ocean, and the depth of the ocean floor, is available as the output variable \textsf{\footnotesize 2Dgrid\_topo} (in both 2D and 3D \textbf{netCDF} files). In terms of the model grid definition, the relevant section of the Northern Hemisphere looks like:

\vspace{-2mm}\footnotesize\begin{verbatim}
  91   9   5   5   8  10  11  11
  12   9   6   7  12  94  94  94
   6   9  12  93  93  94  94  94
   4   6  93  93  93  94  94  94
   3   3  11  93  93  94  94  94
   3   2   9  93  93  94  94  94
   4   2  93  10  10  94  94  94
   3   2  12  10   8   8  11  94
   1   3  93  93  12  11  94  94
   1   5  93  93  94  94  94  94
   1   9  93  93  94  94  94  91
   1  12  93  93  94  94  94  91
   2  12  93  93  94  94  94  91
   2  10  93  93  94  94  94  91
   1   7  93  93  94  94  94  91
   2   5  93  93  93  93  91  91
   2   3   5   8  93  93  91  11
   2   2   2   4  93  93  91   7
\end{verbatim}\normalsize\vspace{-2mm}
with the Mediterranean just above the middle of this section of model grid. Zooming in, the Mediterranean region (plus 2 columns of Atlantic on the left, and a single column of 'land' (the $94$s) on the right) looks like:

\vspace{-2mm}\small\begin{verbatim}
   4   2  93  10  10  94  94  94
   3   2  12  10   8   8  11  94
   1   3  93  93  12  11  94  94
\end{verbatim}\normalsize\vspace{-2mm}
a total of 9(!) grid points including the gateway.

Here, the depth of the ocean at any location is indicated by its ‘k’ value – a number between $1$ and $16$, with $16$ being the surface layer of the ocean, and $1$ the maximum possible depth anywhere. How the ‘k’ values translate into ocean depth is given in Figure \ref{fig:ch3-kgrid}.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent \textbf{Q.} How important is sill depth separating the Mediterranean from the Atlantic?

\vspace{1mm}
In the default configuration of \textbf{cookie}, the sill is defined as being at level $12$ in the ocean circulation model, which corresponds to $558 m$ water depth (Figure \ref{fig:ch3-kgrid}). This is simple to adjust and explore the consequences of it being deep or more shallow. (In contrast, changing the land-sea mask and e.g., widening the gateway is much more involved, although still possible.)

\vspace{1mm}
\textit{User-config} \textsf{\footnotesize LAB.2.6.3} includes a new parameter setting:
\vspace{-2mm}\small\begin{verbatim}
# specify the 'topography' file for the ocean circulation model
# this is file: 'saltages.k1' in directory genie-paleo/p_worjh2
# NOTE: values 1-16 are levels in the ocean circulation model
#       1  == deepest (5000 m)
#       16 == shallowest/surface (80 m)
go_topo='saltages'
\end{verbatim}\normalsize\vspace{-2mm}

The parameter: \texttt{\small go\_topo} points to a file in directory \textsf{\footnotesize genie-paleo/p\_worjh2} called \textsf{\footnotesize saltages.k1} (the parameter value string omits the \texttt{\small .k1} bit). If you go find this and open it up in a text editor, you will find an array of integers looking like Figure \ref{fig:ch3-ijgrid}. You can change the sill depth number ... save the file (\uline{do not} rename it in this case), and then run the experiment! Because the grid point immediately to the East of the gateway ($12$) has a value of $10$, it make no sense to try values lower than $10$. The largest value you can use is $16$. So, \uline{pick an integer value between $10$ and $16$ inclusive} (remembering that $12$ is the default (== unchanged)).

In this example, there is no \textit{re-start} to start your experiment from, so you will need to run it for much longer -- $2000$ years is probably the minimum useful integration time. Your experiment run command will then look like:

\vspace{-2mm}\small\begin{verbatim}
$ ./runcookie.sh cookie.C.p_worjh2.r LABS LAB.2.6.3 2000
\end{verbatim}\normalsize\vspace{-2mm}

\vspace{1mm}
\noindent \uline{Run long experiments by submitting them to the cluster queue}.

\vspace{1mm}
\noindent Lastly, you will need a control experiment because you are 'only' running for $2,000$ years and hence your end state will not be exactly like provided re-start experiments which have been run for $10,000$ years. To create one: copy-rename-edit \textsf{\footnotesize LAB.2.6.3} to e.g., \textsf{\footnotesize LAB.2.6.3.control} and then comment out the line that specifies a different '.k1' file:

\vspace{-2mm}\small\begin{verbatim}
#go_topo='saltages'
\end{verbatim}\normalsize\vspace{-2mm}
(and run for $2,000$ years also).

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent \textbf{Q.} How does seawater exchange between the Mediterranean from the Atlantic depend on the sill depth? What are the consequences of changes in this exchange for the salinity properties of the Mediterranean?

Because the Mediterranean Sea in the model is 3-dimensional (rather than e.g., a single box), in order to answer this, you should look for representative properties that you can read off from the model output (or derive/calculate) that can be contrasted (or graphed) in a quick and easy way against 'k' value (or sill depth in $m$). For instance:

\begin{enumerate}[noitemsep]
\setlength{\itemindent}{.0in}
\vspace{1mm}
\item Mean surface salinity in the Mediterranean (i.e., average the salinity values in the ocean surface layer in the model just for grid points comprising the Mediterranean.
\\Note: with a little programming, you can calculate the mean salinity throughout the Mediterranean, or the total salt content.
\vspace{1mm}
\item The surface seawater velocity into the Mediterranean through the gateway.
\\Better would be to calculate the maximum integrated volumetric flow in units of $Sv$, as in the over-turning stream-function and AMOC 'maximum' (but this is a little more involved).
\vspace{1mm}
\item If we were simulating proxies such as strontium (or carbon, calcium) isotopes, changes in the mean isotopic composition of the Mediterranean would reflect changing connectivity with the Atlantic(?)
\vspace{1mm}
\item If we were simulating the carbon cycle, then carbonate saturation or pH might be useful indicators of Mediterranean Sea changes.
\end{enumerate}
(Try and think of some other measures that you could use to quantify the impact of changing sill depth and gateway connectivity.)

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\noindent If you wanted, you can combine an altered gateway sill depth with a salinity (or freshwater) forcing, by adding (copy-paste) the \textit{forcing} parameter setting block from \textsf{\footnotesize LAB.2.6.1}:

\vspace{-2mm}\footnotesize\begin{verbatim}
#
# *** FORCINGS *********************************************************
#
# apply salinity anomoly to the Eastern Mediterranean surface
bg_par_forcing_name="p_worxx2.Fsal_SUR_EMed"
# scale the salinity forcing in units of Sv
# NOTE: to represent freshwater (rather than salinity) addition, make the value NEGATIVE
bg_par_ocn_force_scale_val_2=0.0
# scale the duration of the forcing (years)
bg_par_ocn_force_scale_time_2=0.0
\end{verbatim}\normalsize\vspace{-2mm}

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%------------------------------------------------
\newpage
%------------------------------------------------

\subsection{Dye-tracing Mediterranean outflow}

The ocean biogeochemistry module (\textbf{BIOGEM}) in \textbf{cookie} provides a framework for applying time- and spatially-variable ‘forcings’ of the Earth system\footnote{Refer to the '\textit{force the system}' \textsf{HOW-TO} in the cookie manual for further details on \textit{forcings}.} – fluxes or 'restored-to' boundary conditions that can be prescribed for any gas, dissolved substance (including temperature and salinity), or particulate matter. Examples include freshwater input (== a negative salinity flux forcing) of the North Atlantic to alter ocean circulation, fossil fuel \(CO_{2}\) emissions to the atmosphere (== a \(CO_{2}\) gas flux forcing), or aeolian iron supply to the surface ocean (a 2-D dust flux forcing).

\vspace{1mm}
For example: view the \textit{user-config} file: \textsf{\footnotesize LAB.2.6.3} –- you will see the following lines (under the heading: ‘\texttt{\# --- FORCINGS ---}’)

\vspace{-2mm}\small
\begin{verbatim}
bg_par_forcing_name="pyyyyz.Fblue"
bg_par_force_point_i=22
bg_par_force_point_j=33
bg_par_force_point_k=8
bg_par_ocn_force_scale_val_49=1.0E12
\end{verbatim}
\normalsize\vspace{-2mm}

The first line points \textbf{cookie} to a directory located in \textsf{\footnotesize cgenie.cookie/genie-forcings} that contains a set of files that define what geochemical property is going to be altered plus information about how the magnitude of the forcing changes with time.

There are then three lines (\texttt{bg\_par\_force\_point\_i=20}, ...) that specify the location in the ocean of the geochemical forcing that is going to be applied. The point sources are specified in (i,j,k) coordinates, which in this case is (22,33,08). For the ocean model resolution we are using, the grid is 36x36x16, and in which: longitude (i) is counted from left-to-right (1 to 36); latitude (j) is counted from bottom-to-top (1 to 36); level depth (k) is counted from downwards top-to-bottom (16 down to 1). Thus, (22,33,08) is a release of tracer in the North Atlantic, a little south of Greenland, and intermediate depth (level = 8 out of 16). Refer to the Figures for how the horizontal (Figure \ref{fig:ch3-ijgrid}) and vertical (Figure \ref{fig:ch3-kgrid}) grid is specified.

Finally, there is a scaling parameter (\texttt{bg\_par\_ocn\_force\_scale\_val\_49}) which modifies the magnitude of the flux to be applied\footnote{Flux \textit{forcings} in \textbf{cookie} are in units of \(mol yr^{-1}\) per model grid point.} -- the default value in the forcing definition itself is zero \(0.0 mol yr^{-1}\) (so you will need to set something here!).

\noindent You are going to run a brief experiment in which you will be injecting a conservative ‘dye’ tracer into the ocean. The \textbf{BIOGEM} module has two tracers that can be defined for this purpose – ‘blue’ and ‘red’ -- you will be using the \uline{blue} one here. You can control the flux of blue dye by editing the flux scaling parameter:

\vspace{-2mm}\small\begin{verbatim}
bg_par_ocn_force_scale_val_49=1.0E12
\end{verbatim}\normalsize\vspace{-2mm}

\vspace{1mm}
Note that the \textit{base-config} you will be using (\textsf{\footnotesize cookie.C.p\_worjh2.rb}) is different from previously.

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent Run the model for … whatever, 50 years will do. Use the \textit{re-start} experiment that you have just downloaded to start from:

\vspace{-2mm}\small\begin{verbatim}
$ ./runcookie.sh cookie.C.p_worjh2.rb LABS LAB.2.6.3 50 cookie.C.p_worjh2.r.SPIN
\end{verbatim}\normalsize\vspace{-2mm}

View the results – for instance how the blue tracer distribution evolves with time – in the \textit{time-slice} files (full ocean/atmosphere) properties saved in the \textbf{netCDF} format (\texttt{.nc}) files). You can follow the progress of the dye (and hence diagnose the properties of ocean circulation in the model) by plotting vertical and/or horizontal slices that go through (or near) the cell location in which you inject the dye tracer in the 3D \textbf{netCDF} file. Note that \textbf{Panoply} appears to ‘count’ the ocean layers in the opposite direction to the way in which the ocean model is actually counting them – the correct definition is with ‘1’ being very deepest level possible (and as displayed in the figure) and '16' is the surface.

You can also view the tracer distributions in terms of a water-column integrated tracer inventory (\textbf{netCDF} variable name: \textsf{\footnotesize ocn\_int\_colb}; long name: \textsf{\footnotesize colb water-column integrated tracer inventory}) in the 2D \textbf{netCDF} output. (See: \textit{Sabine et al.} [2004] for the use of water column integrals in the context of the distribution of anthropogenic \(CO_{2}\) uptake and storage.) Changes in tracer inventory with time can be tracked in the time-series file: \textsf{\footnotesize biogem\_series\_ocn\_colb.res}

Spend a little while altering the flux (\texttt{\small bg\_par\_ocn\_force\_scale\_val\_49}) and/or location
\\(\texttt{\small bg\_par\_force\_point\_i}, \texttt{\small bg\_par\_force\_point\_j, bg\_par\_force\_point\_k}) of tracer input. Overall -- note how you can use numerical ‘tracers’ to help diagnose (and better understand) the circulation of the ocean.

\large\uline{Ignore the 'red' tracer for now ... we'll look at that shortly.}\normalsize

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent When you add the numerical dye, particularly early on in the experiment, you may see a 'front' of \uline{negative} tracer concentrations leading the (positive) tracer as it spreads. DON'T PANIC!

The model is ... a model (of the numerical flavor) and not an exact analytical solution. So errors in how it solves ocean transport are to be expected.

Moreover, by default the ocean circulation model employs an isopycnal/diapycnal mixing scheme. This can lead to unwanted negative tracer values when sharp horizontal (or vertical) transitions in concentration occur. (In this example, e.g. by injection dye at a point location into a surrounding ocean of initially zero concentration.)

You can change to a simpler horizontal/vertical (.false.) mixing scheme by adding to the \textit{user-config} file:

\vspace{-2mm}\small\begin{verbatim}
# turn OFF (=.false.) isopycnal/diapycnal mixing
go_diso=.false.
\end{verbatim}\normalsize\vspace{-2mm}

If you try this, you should (hopefully) find much less (or no) negative tracer concentrations occur. However, also note that by changing the physics of ocean mixing, you also slightly alter the large-scale circulation of the ocean (and e.g. the AMOC might change slightly in strength).\footnote{You might contrast the overturning stream-functions for experiments run both with and without horizontal/vertical mixing.} 

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

\noindent Lastly, an interesting (honest!) and illustrative exercise is to use the dye tracer to pick out the path taken by Mediterranean Intermediate Water. Despite the low resolution of the \textbf{cookie} ocean circulation model component and the highly restricted representation of the Mediterranean, the model does project a salty Mediterranean as a consequence of  P-E across this basin (and its catchments) being negative and this higher density water makes its way out in the subsurface into the Atlantic.

To do this -- simply specify a dye injection somewhere in the Mediterranean (be careful with the restricted depth of the Mediterranean – if you inject too deeply (into the crust!) then you will not see anything (refer to the figure for the depth level (k) number of the maximum depth of the water column in each location), and it is better to inject it relatively close to the opening of the gateway (try some different locations and see which ones produce a reasonably instructive tracing of Mediterranean outflow). Run for e.g., 20 or 50 years (from the provided spin-up). Then:

\vspace{1mm}
\begin{enumerate}[noitemsep]
\vspace{1mm}
\item View the dye-tagged plume of Mediterranean Intermediate Water by plotting a lat-lon slice (from the 3D \textbf{netCDF} file). This will give you the depth of the plume. How does this compare with salinity observations (salinity observations and appropriate global data-sets can be found on the web with a little patience)? You can also view the water-column integrated distribution (2D \textbf{netCDF}).
\vspace{1mm}
\item Try viewing the plume via a lat-depth slice. Refer to the figure to determine the ‘i’ value up the Atlantic that will just graze the edge of what passes for Spain at this low model resolution. Which direction does it head after exiting the Mediterranean? Is this ‘realistic’?
\end{enumerate}
\vspace{1mm}

%------------------------------------------------
\newpage
%------------------------------------------------

\section{Further ideas}

%------------------------------------------------

\subsection{Forcing stronger deep-water formation (‘anti-hosing’)}

There are questions concerning past changes in the AMOC as to whether it is ‘pushed’ or ‘pulled’. i.e., if the AMOC shoals in depth and/or weakens, is it because its production has weakened, or as Antarctic Bottom water (AABW) strengthened and ‘pushed’ it out of the way (to shallower depths)?

What you might try then is to inject salt in the Southern Ocean as opposed to fresh water in the North Atlantic. For this, you can change the name in the \textit{user-config} of the \textit{forcing} used, to:

\vspace{-2mm}\small\begin{verbatim}
bg_par_forcing_name="p_worxx2.Fsal_SUR_SO"
\end{verbatim}\normalsize\vspace{-2mm}

and use the same flux forcing scaling parameter as before:

\vspace{-2mm}\small\begin{verbatim}
bg_par_ocn_force_scale_val_2=0.0
\end{verbatim}\normalsize\vspace{-2mm}

Remember:
\\negative == salt addition (and nominally stronger deep-water formation)
\\positive == freshwater addition (and nominally weaker deep-water formation)

\vspace{1mm}
\noindent \textbf{Is the AMOC (for the same magnitude of forcing) more sensitive to being ‘pushed’ or ‘pulled’?} (Obviously the answer will  depend on where the perturbations are being applied.)

%------------------------------------------------
\vspace{1mm}\noindent\rule{4cm}{0.5pt}\vspace{2mm}
%------------------------------------------------

%----------------------------------------------------------------------------------------
%----------------------------------------------------------------------------------------